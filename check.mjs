import * as countup from 'react-countup';
console.log('Countup keys:', Object.keys(countup));
import * as typewriter from 'react-simple-typewriter';
console.log('Typewriter keys:', Object.keys(typewriter));
